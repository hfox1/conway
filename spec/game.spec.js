const Game = require('../game.js');

describe('Game', () => {
  it('prompts for a starting board', () => {});
  it('says starting board stored', () => {});
  it('logs the evolution of the board', () => {});
});
